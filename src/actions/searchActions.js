const searchInitialState={

};

const searchActions=(state=searchInitialState,action)=>{
    switch(action.type) {
        case "SEARCH_MOVIES":
            break;
        default:
            break;
    }
}